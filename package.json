{
  "name": "ntk-gallery",
  "version": "1.0.0",
  "description": "NTK Speakers Gallery - A visually stunning web application showcasing NTK conference speakers",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "start": "concurrently \"npm run dev --prefix backend\" \"npm start --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "test": "npm test --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm start --prefix frontend\"",
    "build-backend": "npm run build --prefix backend",
    "build-frontend": "npm run build --prefix frontend"
  },
  "keywords": ["ntk", "speakers", "gallery", "react", "nodejs", "conference"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}